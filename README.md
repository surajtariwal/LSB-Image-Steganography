# LSB-Image-Steganography
This project implements LSB (Least Significant Bit) steganography using the C programming language. It allows you to encode a secret text file inside a BMP image and later decode it back without altering the visible quality of the image. Both encoding and decoding are implemented from scratch using C file operations and bitwise manipulation.
